# Reshade-HDR-Simulate-Oversaturation
ReShade shader that simulates the oversaturated look of narrow-gamut (sRGB) content misread as native wide-gamut on HDR displays, using real primary matrices instead of generic saturation/vibrance boosts.
